# Personal Organizer

Персональный органайзер с календарем и управлением задачами.

## Демо

Сайт развёрнут на Vercel: **https://personal-organizer-gamma.vercel.app**

## Стек технологий

- **Next.js 14** - React фреймворк с App Router
- **React 18** - библиотека UI
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация
- **Prisma** - ORM для работы с базой данных
- **PostgreSQL (Neon)** - база данных (синхронизация между устройствами)
- **date-fns** - работа с датами
- **lucide-react** - иконки
- **next-pwa** - поддержка PWA
- **web-push** - push-уведомления

## Функции

### Основные
- 📅 Интерактивный календарь с навигацией по месяцам
- ✅ Создание, редактирование и удаление задач
- 🏷️ Категории для задач (Работа, Личное, Учеба, Здоровье)
- 🔴 Приоритеты задач (низкий, средний, высокий)
- 📊 Статусы задач (todo, in_progress, done)

### Drag & Drop
- 🎯 **В списке задач** - перетаскивайте для изменения порядка
- 📆 **В календаре** - перетаскивайте задачи между днями
- 📋 **В чеклисте** - перетаскивайте подзадачи

### Подзадачи (Чеклист)
- ✓ Создавайте подзадачи внутри основной задачи
- ✓ Отмечайте выполненные галочками
- ✓ Следите за прогрессом выполнения (прогресс-бар)
- ✓ Перетаскивайте для изменения порядка

### Синхронизация и PWA
- 📱 **Синхронизация между устройствами** - данные хранятся в облаке (PostgreSQL)
- 📲 **PWA** - установите приложение на рабочий стол (Android/iOS/Windows/Mac)
- 🔔 **Push-уведомления** - получайте напоминания о задачах
- 💾 **Офлайн-режим** - работает без интернета (кэширование)

### Установка на телефон
1. Откройте сайт в Chrome/Safari
2. Нажмите "Установить" или "Добавить на главный экран"
3. Приложение появится на рабочем столе!

## Установка для разработки

1. Клонируй репозиторий:
```bash
git clone <repository-url>
cd personalOrganizer
```

2. Установи зависимости:
```bash
npm install
```

3. Настрой базу данных:
   - Создай проект на [neon.tech](https://neon.tech)
   - Скопируй DATABASE_URL
   - Создай файл `.env` (см. `.env.example`):
   ```env
   DATABASE_URL="postgresql://..."
   ```

4. Примени миграции:
```bash
npx prisma migrate dev --name init
```

5. Заполни начальные категории:
```bash
npx ts-node prisma/seed.ts
```

6. (Опционально) Настрой push-уведомления:
   - Сгенерируй VAPID ключи:
   ```bash
   npx web-push generate-vapid-keys
   ```
   - Добавь ключи в `.env`:
   ```env
   VAPID_PUBLIC_KEY="your_public_key"
   VAPID_PRIVATE_KEY="your_private_key"
   ```

7. Запусти dev сервер:
```bash
npm run dev
```

8. Открой [http://localhost:3000](http://localhost:3000)

## Команды

```bash
npm run dev          # Запуск dev сервера
npm run build        # Сборка для production
npm run start        # Запуск production сервера
npm run lint         # Проверка кода
npm run db:migrate   # Применить миграции
npm run db:generate  # Сгенерировать Prisma клиент
npm run db:studio    # Открыть Prisma Studio
```

## Деплой на Vercel

1. Подключи GitHub репозиторий на [vercel.com](https://vercel.com)
2. Добавь переменные окружения:
   - **Name:** `DATABASE_URL`
   - **Value:** строка подключения из Neon
   - **Name:** `VAPID_PUBLIC_KEY` (опционально, для push-уведомлений)
   - **Value:** публичный VAPID ключ
   - **Name:** `VAPID_PRIVATE_KEY` (опционально, для push-уведомлений)
   - **Value:** приватный VAPID ключ
3. Нажми **Deploy**
4. Готово! Сайт автоматически соберётся и развернётся

## Структура проекта

```
personal-organizer/
├── prisma/
│   ├── schema.prisma     # Схема базы данных
│   ├── seed.ts           # Начальные данные
│   └── migrations/       # Миграции базы данных
├── public/
│   ├── manifest.json     # PWA манифест
│   └── icons/            # Иконки приложения
├── src/
│   ├── app/
│   │   ├── api/          # API routes
│   │   │   ├── tasks/    # CRUD задач
│   │   │   ├── categories/# CRUD категорий
│   │   │   ├── subtasks/ # CRUD подзадач
│   │   │   └── push/     # Push-уведомления
│   │   ├── page.tsx      # Главная страница
│   │   └── layout.tsx    # Корневой layout
│   ├── components/       # React компоненты
│   │   ├── Calendar.tsx
│   │   ├── TaskList.tsx
│   │   ├── DayView.tsx
│   │   ├── SubtaskList.tsx    # Чеклист подзадач
│   │   ├── QuickAddTask.tsx
│   │   ├── CreateTaskButton.tsx
│   │   └── PushNotificationManager.tsx
│   └── lib/
│       └── db.ts         # Подключение к Prisma
├── .env.example          # Пример переменных окружения
├── README.md             # Этот файл
├── AGENTS.md             # Инструкции для AI-агентов
├── RUN.md                # Подробная инструкция по запуску
└── package.json
```

## Хранение данных

Данные сохраняются в **PostgreSQL базе на Neon**:
- ✅ Доступно с любого устройства
- ✅ Данные не пропадают при очистке браузера
- ✅ Автоматические бэкапы
- 💾 Офлайн-режим (кэширование с помощью Service Worker)

## Дальнейшее развитие

- [x] Drag & drop для задач
- [x] Деплой на Vercel
- [x] Переход на PostgreSQL с синхронизацией
- [x] Подзадачи (чеклист)
- [x] PWA - установка на телефон
- [x] Push-уведомления
- [ ] Telegram бот для уведомлений
- [ ] Повторяющиеся задачи
- [ ] Экспорт/импорт данных
- [ ] Темная тема
- [ ] Статистика и аналитика
